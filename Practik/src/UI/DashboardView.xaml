<UserControl x:Class="UI.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <materialDesign:Card Grid.Row="0" Padding="16" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="{Binding Title}" Style="{StaticResource MaterialDesignHeadline5TextBlock}"/>
                <TextBlock Text="{Binding Subtitle}" Style="{StaticResource MaterialDesignSubtitle1TextBlock}" Opacity="0.85"/>
                <Button Content="Refresh" Command="{Binding RefreshCommand}" Margin="0,12,0,0" HorizontalAlignment="Left"/>
            </StackPanel>
        </materialDesign:Card>

        <UniformGrid Grid.Row="1" Columns="3" HorizontalAlignment="Left">
            <materialDesign:Card Margin="0,0,16,16" Padding="16" Width="260" Visibility="{Binding CanWorkWithClients, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel>
                    <TextBlock Text="Клієнти" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="0,0,0,8"/>
                    <TextBlock Text="{Binding ClientsCount, StringFormat=Всього: {0}}" Margin="0,0,0,8"/>
                    <ItemsControl ItemsSource="{Binding RecentClients}" Margin="0,0,0,16">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding DisplayName}" TextTrimming="CharacterEllipsis"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <Button Content="Відкрити" Command="{Binding GoToClientsCommand}"/>
                </StackPanel>
            </materialDesign:Card>

            <materialDesign:Card Margin="0,0,16,16" Padding="16" Width="260" Visibility="{Binding CanWorkWithOrders, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel>
                    <TextBlock Text="Замовлення" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="0,0,0,8"/>
                    <TextBlock Text="{Binding OrdersCount, StringFormat=Всього: {0}}" Margin="0,0,0,8"/>
                    <ItemsControl ItemsSource="{Binding RecentOrders}" Margin="0,0,0,16">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="0,0,0,6">
                                    <TextBlock Text="{Binding OrderId, StringFormat=№{0}}" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding Status}" Opacity="0.85"/>
                                    <TextBlock Text="{Binding TechnicianName}" Opacity="0.8"/>
                                    <TextBlock Text="{Binding ClientName}" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding DeviceTypeName}" Opacity="0.8"/>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <Button Content="Відкрити" Command="{Binding GoToOrdersCommand}"/>
                </StackPanel>
            </materialDesign:Card>
        </UniformGrid>
    </Grid>
</UserControl>
