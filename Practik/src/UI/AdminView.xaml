<UserControl x:Class="UI.AdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UI"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <TabControl>
        <TabItem Header="Create User">
            <Grid>
                <materialDesign:Card Padding="32" Margin="16" VerticalAlignment="Center" HorizontalAlignment="Center" Width="350">
                    <StackPanel>
                        <TextBlock Text="Create User" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="0 0 0 16" HorizontalAlignment="Center"/>
                        <TextBox materialDesign:HintAssist.Hint="Username" Style="{StaticResource MaterialDesignOutlinedTextBox}" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0 0 0 8"/>
                        <PasswordBox x:Name="CreateUserPasswordBox" materialDesign:HintAssist.Hint="Password" Style="{StaticResource MaterialDesignOutlinedPasswordBox}" local:PasswordBoxHelper.BoundPassword="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0 0 0 8"/>
                        <TextBox materialDesign:HintAssist.Hint="First Name" Style="{StaticResource MaterialDesignOutlinedTextBox}" Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}" Margin="0 0 0 8"/>
                        <TextBox materialDesign:HintAssist.Hint="Last Name" Style="{StaticResource MaterialDesignOutlinedTextBox}" Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}" Margin="0 0 0 8"/>
                        <ComboBox materialDesign:HintAssist.Hint="Role" Style="{StaticResource MaterialDesignOutlinedComboBox}" SelectedValue="{Binding Role, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" SelectedValuePath="Content" SelectedIndex="2" Margin="0 0 0 16">
                            <ComboBoxItem Content="Administrator"/>
                            <ComboBoxItem Content="Master"/>
                            <ComboBoxItem Content="User"/>
                        </ComboBox>
                        <Button Content="Create User" Command="{Binding CreateUserCommand}" CommandParameter="{Binding ElementName=CreateUserPasswordBox}" IsEnabled="True"/>
                    </StackPanel>
                </materialDesign:Card>
            </Grid>
        </TabItem>

        <TabItem Header="Manage Roles">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <DataGrid Grid.Row="0"
                          ItemsSource="{Binding Users}"
                          SelectedItem="{Binding SelectedUser}"
                          AutoGenerateColumns="False"
                          Style="{StaticResource MaterialDesignDataGrid}"
                          CanUserAddRows="False"
                          HeadersVisibility="All"
                          LoadingRow="DataGrid_LoadingRow">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="*"/>
                        <DataGridTextColumn Header="First Name" Binding="{Binding FirstName}" Width="*"/>
                        <DataGridTextColumn Header="Last Name" Binding="{Binding LastName}" Width="*"/>
                        <DataGridTextColumn Header="Role" Binding="{Binding Role.Code}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="16">
                    <Button Content="Refresh" Width="120" Margin="5" Command="{Binding RefreshUsersCommand}"/>
                    <ComboBox Style="{StaticResource MaterialDesignOutlinedComboBox}"
                              materialDesign:HintAssist.Hint="Role"
                              Width="180"
                              Margin="5"
                              ItemsSource="{Binding Roles}"
                              DisplayMemberPath="Code"
                              SelectedItem="{Binding SelectedUserRole}"/>
                    <Button Content="Save" Width="120" Margin="5" Command="{Binding UpdateUserRoleCommand}"/>
                    <Button Content="Delete User" Width="140" Margin="5" Command="{Binding DeleteUserCommand}"/>
                </StackPanel>
            </Grid>
        </TabItem>

        <TabItem Header="Assign Technician">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <DataGrid Grid.Row="0"
                          ItemsSource="{Binding RepairOrders}"
                          SelectedItem="{Binding SelectedRepairOrder}"
                          AutoGenerateColumns="False"
                          Style="{StaticResource MaterialDesignDataGrid}"
                          CanUserAddRows="False"
                          HeadersVisibility="All"
                          LoadingRow="DataGrid_LoadingRow">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Client" Binding="{Binding ClientName}" Width="220"/>
                        <DataGridTextColumn Header="Type" Binding="{Binding DeviceTypeName}" Width="140"/>
                        <DataGridTextColumn Header="Manufacturer" Binding="{Binding Manufacturer}" Width="160"/>
                        <DataGridTextColumn Header="Model" Binding="{Binding Model}" Width="160"/>
                        <DataGridTextColumn Header="Technician" Binding="{Binding TechnicianName}" Width="140"/>
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}"/>
                        <DataGridTextColumn Header="Problem Description" Binding="{Binding ProblemDescription}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="16">
                    <ComboBox Style="{StaticResource MaterialDesignOutlinedComboBox}" materialDesign:HintAssist.Hint="Technician" Width="150" Margin="5" ItemsSource="{Binding Technicians}" DisplayMemberPath="Username" SelectedValuePath="UserId" SelectedValue="{Binding SelectedTechnicianId}"/>
                    <Button Content="Assign" Width="100" Margin="5" Command="{Binding AssignTechnicianCommand}"/>
                </StackPanel>
            </Grid>
        </TabItem>
    </TabControl>
</UserControl>
