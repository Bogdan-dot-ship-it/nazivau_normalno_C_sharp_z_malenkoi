<UserControl x:Class="UI.OrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="900">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <DataGrid Grid.Column="0"
                  ItemsSource="{Binding Orders}"
                  SelectedItem="{Binding SelectedOrder}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  Style="{StaticResource MaterialDesignDataGrid}"
                  HeadersVisibility="All"
                  LoadingRow="DataGrid_LoadingRow">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Client" Binding="{Binding ClientName}" Width="220"/>
                <DataGridTextColumn Header="Type" Binding="{Binding DeviceTypeName}" Width="140"/>
                <DataGridTextColumn Header="Device" Width="220" Binding="{Binding Manufacturer}"/>
                <DataGridTextColumn Header="Model" Binding="{Binding Model}" Width="160"/>
                <DataGridTextColumn Header="Technician" Binding="{Binding TechnicianName}" Width="140"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120"/>
                <DataGridTextColumn Header="Problem" Binding="{Binding ProblemDescription}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <materialDesign:Card Grid.Column="1" Padding="16" Margin="16,0,0,0" VerticalAlignment="Top">
            <StackPanel>
                <TextBlock Text="Orders" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="0,0,0,16"/>

                <Button Content="Refresh" Command="{Binding RefreshCommand}" Margin="0,0,0,16"/>

                <TextBlock Text="Create new order" Style="{StaticResource MaterialDesignSubtitle1TextBlock}" Margin="0,0,0,8"/>

                <ComboBox Style="{StaticResource MaterialDesignOutlinedComboBox}"
                          materialDesign:HintAssist.Hint="Device"
                          ItemsSource="{Binding Devices}"
                          SelectedItem="{Binding SelectedDevice}"
                          Margin="0,0,0,8">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock>
                                <Run Text="{Binding ClientName}"/>
                                <Run Text=" — "/>
                                <Run Text="{Binding DeviceTypeName}"/>
                                <Run Text=" — "/>
                                <Run Text="{Binding Manufacturer}"/>
                                <Run Text=" "/>
                                <Run Text="{Binding Model}"/>
                            </TextBlock>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
                <TextBox Style="{StaticResource MaterialDesignOutlinedTextBox}" materialDesign:HintAssist.Hint="Problem Description" Text="{Binding ProblemDescription, UpdateSourceTrigger=PropertyChanged}" TextWrapping="Wrap" AcceptsReturn="True" Height="120" Margin="0,0,0,16"/>

                <Button Content="Create" Command="{Binding CreateOrderCommand}" Margin="0,0,0,16"/>

                <TextBlock Text="Manage selected order" Style="{StaticResource MaterialDesignSubtitle1TextBlock}" Margin="0,0,0,8"/>

                <ComboBox Style="{StaticResource MaterialDesignOutlinedComboBox}"
                          materialDesign:HintAssist.Hint="Status"
                          ItemsSource="{Binding Statuses}"
                          SelectedItem="{Binding SelectedStatus}"
                          Margin="0,0,0,8"/>

                <Button Content="Update Status" Command="{Binding UpdateStatusCommand}" Margin="0,0,0,8"/>
                <Button Content="Mark Completed" Command="{Binding MarkCompletedCommand}" Margin="0,0,0,8"/>
                <Button Content="Delete" Command="{Binding DeleteOrderCommand}"/>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>
