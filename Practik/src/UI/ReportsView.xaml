<UserControl x:Class="UI.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="900">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <materialDesign:Card Grid.Column="0" Padding="16" Margin="0,0,16,0" VerticalAlignment="Top">
            <StackPanel>
                <TextBlock Text="Reports" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="0,0,0,16"/>

                <Button Content="Refresh" Command="{Binding RefreshCommand}" Margin="0,0,0,16"/>

                <TextBlock Text="Select completed order" Style="{StaticResource MaterialDesignSubtitle1TextBlock}" Margin="0,0,0,8"/>

                <ComboBox Style="{StaticResource MaterialDesignOutlinedComboBox}"
                          ItemsSource="{Binding CompletedOrders}"
                          SelectedItem="{Binding SelectedOrder}"
                          Margin="0,0,0,16">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock TextTrimming="CharacterEllipsis">
                                <Run Text="№"/>
                                <Run Text="{Binding OrderId}"/>
                                <Run Text=" — "/>
                                <Run Text="{Binding ClientName}"/>
                                <Run Text=" — "/>
                                <Run Text="{Binding DeviceTypeName}"/>
                                <Run Text=" "/>
                                <Run Text="{Binding Manufacturer}"/>
                                <Run Text=" "/>
                                <Run Text="{Binding Model}"/>
                            </TextBlock>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <TextBlock Text="Що було зроблено" Style="{StaticResource MaterialDesignSubtitle1TextBlock}" Margin="0,0,0,8"/>
                <TextBox Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Text="{Binding WorkDoneDescription, UpdateSourceTrigger=PropertyChanged}"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         Height="120"
                         Margin="0,0,0,16"/>

                <Button Content="Export" Command="{Binding ExportCommand}" />
            </StackPanel>
        </materialDesign:Card>

        <materialDesign:Card Grid.Column="1" Padding="16" VerticalAlignment="Top">
            <StackPanel>
                <TextBlock Text="Preview" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="0,0,0,16"/>
                <TextBox Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Text="{Binding PreviewText, Mode=OneWay}"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         IsReadOnly="True"
                         Height="360"/>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>
