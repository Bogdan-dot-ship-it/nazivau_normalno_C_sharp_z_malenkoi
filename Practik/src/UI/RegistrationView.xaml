<UserControl x:Class="UI.RegistrationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:UI"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <materialDesign:Card Width="460" Padding="20">
            <StackPanel>
                <TextBlock Text="Registration" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <TextBox materialDesign:HintAssist.Hint="Username" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,10"/>
                <PasswordBox PasswordChanged="PasswordBox_OnPasswordChanged" materialDesign:HintAssist.Hint="Password" Margin="0,10"/>
                <TextBox materialDesign:HintAssist.Hint="First name" Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}" Margin="0,10"/>
                <TextBox materialDesign:HintAssist.Hint="Last name" Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}" Margin="0,10"/>

                <TextBox Text="{Binding ErrorMessage}"
                         IsReadOnly="True"
                         AcceptsReturn="True"
                         BorderThickness="0"
                         Background="Transparent"
                         Foreground="Red"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         MinHeight="0"
                         MaxHeight="160"
                         Margin="0,10"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                    <Button Content="Back" Command="{Binding BackToLoginCommand}" Margin="0,0,10,0" />
                    <Button Content="Register" Command="{Binding RegisterCommand}" />
                </StackPanel>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>
