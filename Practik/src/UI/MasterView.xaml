<UserControl x:Class="UI.MasterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UI"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <DataGrid Grid.Column="0" Margin="0 0 16 0"
                  ItemsSource="{Binding RepairOrders}" 
                  SelectedItem="{Binding SelectedOrder}" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  Style="{StaticResource MaterialDesignDataGrid}"
                  HeadersVisibility="All"
                  LoadingRow="DataGrid_LoadingRow">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Client" Binding="{Binding ClientName}" Width="220"/>
                <DataGridTextColumn Header="Type" Binding="{Binding DeviceTypeName}" Width="140"/>
                <DataGridTextColumn Header="Manufacturer" Binding="{Binding Manufacturer}" Width="160"/>
                <DataGridTextColumn Header="Model" Binding="{Binding Model}" Width="160"/>
                <DataGridTextColumn Header="Technician" Binding="{Binding TechnicianName}" Width="140"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}"/>
                <DataGridTextColumn Header="Problem Description" Binding="{Binding ProblemDescription}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <materialDesign:Card Grid.Column="1" Padding="16" VerticalAlignment="Top">
            <StackPanel>
                <TextBlock Text="Update Status" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="0 0 0 16"/>
                <ComboBox Style="{StaticResource MaterialDesignOutlinedComboBox}" 
                          materialDesign:HintAssist.Hint="New Status" 
                          Margin="0 0 0 16" 
                          ItemsSource="{Binding Statuses}" 
                          SelectedValue="{Binding NewStatus}"/>
                <Button Content="Update Status" 
                        Command="{Binding UpdateStatusCommand}"/>

                <Button Content="Виконано"
                        Margin="0 8 0 0"
                        Command="{Binding MarkCompletedCommand}"/>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>
